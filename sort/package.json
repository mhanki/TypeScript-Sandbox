{
  "name": "sort",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start:build": "tsc -w",
    "start:run": "nodemon build/index.js",
    "start": "concurrently npm:start:*"
  },
  "dev-dependencies": {
    "concurrently": "^7.2.2",
    "nodemon": "^2.0.18"
  }
}
